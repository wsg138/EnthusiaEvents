name: Quality

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  verify:
    name: Maven verify
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Apply final hardening source fix
        if: ${{ hashFiles('.github/apply-final-hardening.py') != '' }}
        run: python .github/apply-final-hardening.py

      - name: Set up Java 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Compile and run quality checks
        run: mvn --batch-mode --no-transfer-progress verify

      - name: Upload plugin jar
        uses: actions/upload-artifact@v4
        with:
          name: EnthusiaEvents-${{ github.sha }}
          path: target/enthusia-events-1.0.0-SNAPSHOT.jar
          if-no-files-found: error

      - name: Commit verified final hardening to main
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && hashFiles('.github/apply-final-hardening.py') != '' }}
        run: |
          rm .github/apply-final-hardening.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m "Apply final BedWars spawn protection ordering [skip ci]"
            git push origin HEAD:main
          fi
